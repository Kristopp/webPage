import { css } from "@emotion/react";

const container = css`
  background-color: var(--background-accent);
  min-height: 10vh;
  padding: 0.5rem;
  display: grid;
`;

const title = css`
margin-left: 1ch
`;

export { container, title };
