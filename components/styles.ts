import { css } from "@emotion/react";

const container = css`
  background-color: rgb(0, 0, 0);
  min-height: 10vh;
  padding: 0.5rem;
  display: grid;
`;

const title = css`
  background-color: rgb(0, 0, 0);
`;

export { container, title };
